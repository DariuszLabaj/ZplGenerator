from typing import Any, Optional


class GraphicSensorCalibration:
	def __init__(self, ) -> None:
		"""
		The ~JG command prints a graph (media sensor profile) of the sensor values
		"""
pass

	def __str__(self) -> str:
		format = f"~JG"
		return format
