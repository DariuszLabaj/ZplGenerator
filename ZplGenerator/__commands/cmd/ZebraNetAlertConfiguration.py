from typing import Any, Optional


class ZebraNetAlertConfiguration:
	def __init__(self, ) -> None:
		"""
		This command returns the table of configured ZebraNet Alert settings to the host.
		"""
pass

	def __str__(self) -> str:
		format = f"~HU"
		return format
