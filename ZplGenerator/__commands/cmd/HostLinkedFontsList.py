from typing import Any, Optional


class HostLinkedFontsList:
	def __init__(self, ) -> None:
		"""
		The ^HT command receives the complete list of font links over a communication port
		"""
pass

	def __str__(self) -> str:
		format = f"^HT"
		return format
