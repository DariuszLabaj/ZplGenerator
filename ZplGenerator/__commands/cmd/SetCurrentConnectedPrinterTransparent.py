from typing import Any, Optional


class SetCurrentConnectedPrinterTransparent:
	def __init__(self, ) -> None:
		"""
		The ~NT command sets the currently connected network printer to be transparent.
		"""
pass

	def __str__(self) -> str:
		format = f"~NT"
		return format
