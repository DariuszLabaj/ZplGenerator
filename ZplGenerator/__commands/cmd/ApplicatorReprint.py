from typing import Any, Optional


class ApplicatorReprint:
	def __init__(self, ) -> None:
		"""
		f the ~PR command is enabled, the last label printed reprints, similar to the applicator asserting the Reprint signal on the applicator port.
		"""
pass

	def __str__(self) -> str:
		format = f"~PR"
		return format
